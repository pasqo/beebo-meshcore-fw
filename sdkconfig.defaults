# Enable OTA rollback: new firmware boots as "pending" and must confirm
# itself valid via esp_ota_mark_app_valid_cancel_rollback(), otherwise
# the bootloader rolls back to the previous firmware on next reboot.
CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE=y
